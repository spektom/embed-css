This is a simple PERL script that helps embedding resources into the CSS file using [data URI scheme](http://en.wikipedia.org/wiki/Data_URI_scheme).

USAGE:

  embed.pl style.css > style_embedded.css

